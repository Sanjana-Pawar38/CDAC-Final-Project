version: "3.9"

services:
  flask-blog:
    build: .
    container_name: flask_blog
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
